---
import SigninForm from "@src/components/auth/SigninForm.astro";
import LogoLayout from "@src/layouts/LogoLayout.astro";
import { changeLanguage, t } from "i18next";

changeLanguage("en");

const title = t("pages.signin.title");
const description = t("pages.signin.description");
---

<LogoLayout title={title} description={description} noindex={true} nofollow={true} requiresAuth={false} jsonLd={null}>
  <SigninForm />
</LogoLayout>
